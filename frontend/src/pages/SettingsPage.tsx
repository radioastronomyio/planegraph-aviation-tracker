import styles from "./PlaceholderPage.module.css";

export function SettingsPage() {
  return (
    <div className={styles.page} data-testid="settings-page">
      <h1>Settings</h1>
      <p>Configuration UI — coming in WU-05.</p>
    </div>
  );
}
